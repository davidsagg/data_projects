[pytest]
testpaths = tests
addopts = --cov=src --cov-report=term-missing --cov-fail-under=75
markers =
    unit: sem I/O externo
    integration: com SQLite em memória
    api: testes de endpoints FastAPI
